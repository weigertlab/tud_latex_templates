# Master thesis

Adapted from the excellent [CGV LaTeX thesis template](https://tu-dresden.de/ing/informatik/smt/cgv/studium/materialien)

Edit the document metadata near the top of `main.tex` and the content in `frontmatter/` and `chapters/`

```sh
make
make clean
```

Requires a LaTeX installation with `tudscr` and Biber
