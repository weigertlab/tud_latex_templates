# Team project

Uses the TU Dresden `tudscrartcl` class with the same corporate-design header as the thesis template

Edit the multi-author metadata and content in `main.tex`

Place figures in `figs/`; `main.tex` contains a two-panel example

```sh
make
make clean
```
